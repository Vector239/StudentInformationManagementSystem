# 学生信息管理系统项目 - AI专属规则
## 核心约束（必须100%严格遵守）
1.  所有操作必须严格基于本项目的现有代码、技术栈、架构进行，**禁止擅自新增任何用户未明确要求的功能、模块、依赖、文件、调试代码、日志文件**。
2.  处理用户需求时，**只完成用户明确提出的修改内容，不得额外添加扩展功能、冗余逻辑、额外注释、非必要的调试代码**，不得过度实现。
3.  禁止擅自修改项目的核心架构、启动逻辑、依赖配置、数据库表结构，除非用户明确提出修改要求。
4.  所有代码生成、修改必须完全适配本项目的现有结构，不得引入项目中未使用的第三方库、框架、语法规范。
5.  排查问题、修复bug时，只针对问题本身进行修复，不得借机新增功能、修改无关代码。
6.  所有输出必须贴合本项目的实际代码，不得臆造项目中不存在的文件、函数、接口、变量。
7.  由于我现在是一名研究生，所以你生成新的代码的时候如果有新的功能（我要求的），需要添加上注释
8.  我的项目注释文档记录在.cursor文件夹里面的readme.md文件中,以后有功能补充请同步这个文件